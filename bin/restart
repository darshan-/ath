#!/bin/bash

PID_FILE='./run/pid'

if [ ! -e $PID_FILE ]
then
    echo "Warning: $PID_FILE does not exist; doing a fresh start rather than restart."
    bin/up
else
    servers=`wc $PID_FILE | awk '{print $1;}'`
    bin/down
    bin/up $servers
fi
